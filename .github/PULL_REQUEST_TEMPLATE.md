## 対応する機能 ID

<!-- REQUIREMENTS.md の F-XXX-n を 1 つ。複数 ID をまとめない（CLAUDE.md §7-4） -->

## 分かったこと

## やったこと

## 次にやること

## 明示事項（該当する場合は必ず書く）

- [ ] スキーマ変更（`supabase/migrations/` に SQL を追加し `pnpm db:types` を実行した）
- [ ] RLS 変更
- [ ] 外部 API 追加
- [ ] 環境変数追加（`.env.example` に追記した）
- [ ] ADR を追加した（`docs/decisions/`）

## 確認

- [ ] `pnpm lint && pnpm typecheck` が通る
- [ ] `pnpm test` が通る
- [ ] 秘密情報（API キー、service role キー）を含んでいない

## フェーズ進捗

Phase N: XX%
