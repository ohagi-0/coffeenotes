# コーヒー記録アプリ 基本設計書（画面設計）

| 項目 | 内容 |
|---|---|
| 文書バージョン | v1.3 |
| 作成日 | 2026-09-21 |
| 正とするもの | 要件は `docs/REQUIREMENTS.md`、見た目の参照は `docs/design/`（公開: https://ohagi-0.github.io/coffeenotes/design/ 。方針 index / Web スマホ mobile / Web PC desktop / iOS ios の 4 ページ）、決定は `docs/decisions/0006`（色・書体）と `0007`（PC 版・iOS 版） |
| 運用 | アジャイルに変更する。画面や部品を変えるときは **モックとこの文書を同じ PR で更新** する。矛盾があればこの文書を優先し、モックを直す |

この文書は Phase 1 以降の画面実装の指示書。Claude Code はここに書かれたトークン・部品・画面仕様に従って実装し、書かれていない見た目上の判断はモックに合わせる。

---

## 1. デザイン原則

コンセプト名は **Espresso & Copper**。深煎り豆の色を地にして、銅のケトルの色を一点だけ置く。

| # | 原則 | 実装で守ること |
|---|---|---|
| P1 | カードの言葉をそのまま使う | 豆名・ロースター名・フレーバーは英字のまま保持し、翻訳しない。豆名は Bodoni Moda で大きく、数字（標高・価格・比率・星）は Manrope で主役に。日本語はラベルと説明に回す |
| P2 | 銅色は操作できる場所だけ | 銅（`--cu`）を使ってよいのは ＋ボタン、主ボタン、星、選択中タブ、リンク、選択中の枠線。装飾・見出し・区切り線には使わない。要確認は琥珀、削除と失敗は錆色で銅と混ぜない |
| P3 | 片手で、親指で | 主操作は画面下 1/3。タップ領域 44px 以上。画面に主ボタンは 1 つ。詳細（レシピ・焙煎）は折りたたみ、星とメモだけで保存できる |
| P4 | 3 状態を必ず持つ | 読み込み・空・失敗（+ 成功トースト）の無い画面はレビューで差し戻す（CLAUDE.md §5.3） |
| P5 | ダーク専用 | ライトテーマは実装しない。`color-scheme: dark` 固定。OS 設定は見ない（ADR 0006） |

---

## 2. デザイントークン

### 2.1 色

| トークン | 値 | 用途 | shadcn 変数へのマッピング |
|---|---|---|---|
| `--bg` Espresso | `#17120F` | 画面の地、下タブ | `--background` |
| `--sf` Roast | `#211A15` | カード、リスト行の面、入力欄 | `--card` `--popover` `--input` |
| `--sf2` Bean | `#2C231C` | セグメントの地、チップの塗り、スケルトン、二次ボタン | `--secondary` `--muted` `--accent` |
| `--line` | `#3B2F26` | 区切り線、枠線、未選択の星 | `--border` |
| `--text` Crema | `#F1E8DA` | 本文、見出し | `--foreground` `--card-foreground` |
| `--mute` Chaff | `#A39181` | 補助文字、ラベル、非選択タブ | `--muted-foreground` |
| `--cu` Copper | `#D48C4E` | 操作要素（P2） | `--primary` `--ring` |
| `--cu2` | `#F0B57A` | 銅の hover / pressed | — |
| `--on-cu` | `#17120F` | 銅の上の文字 | `--primary-foreground` |
| `--ok` | `#8DB36E` | 保存完了トーストのアイコン | — |
| `--warn` Amber | `#E3A94D` | OCR 要確認、注意 | — |
| `--bad` Rust | `#D96A57` | 削除、失敗、必須未入力 | `--destructive` |
| `--card-paper` | `#F6F1E8` | カード画像のプレースホルダ地 | — |

- 平均星でピンを色分けする地図の 4 段階: 4.5 以上 `#E8A968`、4.0 `#C27F49`、3.5 `#8C6A4E`、3.0 以下 `#6E5A4A`。
- 現在地マーカーは `#5B8DD6`（地図上でだけ使う。UI には使わない）。
- 銅の半透明: 選択中カードの地 `rgba(212,140,78,.14)`、レーダーの塗り `rgba(212,140,78,.28)`。

### 2.2 形・間隔・影

| 項目 | 値 |
|---|---|
| 角丸 | 小 5px（フレーバータグ）、中 12〜14px（入力・ボタン・カード）、大 18px（選択肢カード）、22px（ボトムシート上辺）、999（チップ） |
| 画面の横余白 | 20px（コンテンツ幅 335px @375） |
| 縦リズム | 4 / 8 / 12 / 14 / 20px。セクション見出しの上 20px、下 8px |
| タップ領域 | 最小 44px。チップは高さ 34px だが行間で 44px を確保する |
| 影 | 基本は使わない。＋ボタンだけ `0 10px 24px -8px rgba(212,140,78,.8)`、ボトムシートに `0 -20px 40px -20px rgba(0,0,0,.6)` |
| 下タブ | 高さ 84px（うち safe-area 22px）、上に 1px の `--line` |
| 焦点 | `outline: 2px solid var(--cu); outline-offset: 2px` |

### 2.3 書体

| 役割 | 書体 | ウェイト | 使う場所 |
|---|---|---|---|
| 表示（display） | Bodoni Moda（可変、opsz 自動） | 500 | 豆名、ワードマーク、ヒーローの数字。`letter-spacing: -.02em; line-height: .96` |
| 数字（num） | Manrope | 600（星の数値と集計は 800） | 標高・価格・比率・湯温・時間・日付・ステップ表示・英字ラベル。`font-variant-numeric: tabular-nums` |
| UI（ui） | 端末フォント: `"Hiragino Sans","Hiragino Kaku Gothic ProN","Noto Sans JP","Yu Gothic UI",system-ui,sans-serif` | 400 / 500 / 700 | 日本語の見出し・本文・ラベル・メモ |
| 印刷物 | `"Helvetica Neue",Helvetica,Arial` | 700 | カード画像のプレースホルダ内の文字だけ（アプリの書体から独立させる） |

サイズスケール（px）: 30 / 24 / 20 / 16 / 14 / 12 / 11。行間 1.5（表示書体は .96〜1.05）。

- 画面タイトル: UI 700 / 24px
- セクション見出し: UI 700 / 13px
- 一覧の豆名: display / 21px
- 豆詳細の豆名: display / 34px
- ログインのワードマーク: display / 64px
- 本文: UI 400 / 14px、補助: UI 400 / 12px、ラベル: 11px `--mute`
- 星の数値: num 800 / 36px（入力時）、20px（詳細）、13px（一覧）

---

## 3. 共通レイアウト

Web スマホ版は「アプリ」ではなく「スマホ向け Web サイト」の作法で組む（ADR 0007 追記）。下タブは iOS アプリ版だけが持つ。

- 基準幅 375px。767px までは 1 面。768px 以上のレイアウトは §9.1（ADR 0007）。
- サイトヘッダー（sticky、高さ 56px、下に 1px の `--line`）: 左にワードマーク（display 22px）、右に「＋ 記録する」（銅のピル、高さ 36px）とメニューボタン（44px）。ウィザード中は「＋ 記録する」の代わりに「やめる」リンク。
- メニュー: ヘッダー右のボタンで右から開くドロワー（幅 82%）。項目はページ名そのまま: 入力記録一覧 / 記録したお店 / 記録したお店のマップ / 好みの分析 / 設定、「記録を追加」ボタン、最下部にアカウントとログアウト。一文字の略称（「店」「地図」）は使わない。PC のサイドバーも同じ文言。iOS の下タブだけ短縮形（ホーム / マップ / ＋ / 好み / 設定）。
- 画面タイトルはヘッダーの下に UI 700 / 24px。詳細ページはタイトルの代わりにパンくず（`入力記録一覧 › 豆`）と右端の「編集」リンク。ウィザードは 3 段のステッパー（撮影 / 確認 / 店と評価。済みと現在は銅の上線）。
- フッター: 各ページの末尾に `--sf` 地のサイトフッター。ワードマーク、主要ページへのリンク、著作権・プライバシー・お問い合わせ。
- 戻るはブラウザの戻る。画面内に独自の戻るボタンは置かない。各画面は共有できる URL を持つ。
- 地図はページ内の 300px の枠に置き、選んだ店のカードと一覧はその下に普通に流す。ボトムシートは使わない。
- 撮影は OS のカメラ（`<input capture>` 相当を `src/lib/platform/camera.ts` 経由で）。表と裏のタイルに「カメラで撮る / 写真を選ぶ」。
- iOS アプリ版のナビは §9.3（下タブ + 大タイトル）。同じナビ定義から、Web ではヘッダー + ドロワー、ネイティブでは下タブを描く。

---

## 4. 部品仕様

shadcn/ui の生成物（`src/components/ui/`）は手で編集せず、以下は `src/components/` 配下に独自部品として作る。

| 部品 | ファイル案 | 仕様 |
|---|---|---|
| 星評価 `RatingStars` | `components/logs/rating-stars.tsx` | 5 つの星。表示は 15 / 22 / 44px の 3 サイズ。入力モードでは各星の左半分タップで `.5`、右半分で `.0`。値は `numeric(2,1)`、1.0〜5.0。隣に数値（num 800）を常に表示。`role="slider"` と `aria-valuenow` を付け、左右キーで 0.5 ずつ動く（スライダー入力の要件） |
| 味覚チャート `TasteDots` | `components/beans/taste-dots.tsx` | 5 軸固定（Flavor / Sweetness / Acidity / After taste / Body）。各軸 5 つの 20px 円。塗りは銅。タップで入力、同じ値をもう一度タップで未入力（NULL）に戻す。右端に値（num）または `—` |
| 味覚レーダー `TasteRadar` | `components/beans/taste-radar.tsx` | SVG 200×190。5 段のグリッド、値のポリゴン（塗り銅 28%、線銅 2px）、頂点に 3px の円。統計では全体平均を `--mute` の点線で重ねる |
| 要確認フィールド | `components/logs/ocr-field.tsx` | `confidence < LOW_CONFIDENCE_THRESHOLD`（`src/lib/schemas/bean-card.ts`、現在 0.6）の項目に琥珀の点線下線（2px、offset 5px）とラベル横の「要確認」タグ（琥珀地、10px、700）。ユーザーが編集したら解除 |
| 絞り込みチップ | `components/filter-chips.tsx` | 高さ 34px、角丸 999、枠 `--line`。選択中は文字色反転（地 `--text`、文字 `--bg`）。先頭の「絞り込み」だけ銅の枠線（操作を開く）。横スクロール、画面端まで余白を突き抜ける |
| セグメント | `components/logs/place-segment.tsx` | 店で / 自宅で。地 `--sf2`、選択側は `--text` の塗り |
| 入力欄 | shadcn `Input` をトークンで上書き | 高さ 48px、角丸 12px、地 `--sf`、枠 `--line`。プレースホルダは `--mute` |
| ボタン | shadcn `Button` のバリアント | 主: 銅地、高さ 52px、角丸 14px、700。二次: `--sf2` 地。ゴースト: 枠 `--line`。破壊: 枠 `--bad` 文字 `--bad`、必ず確認ダイアログ。白: Google ログイン専用 |
| 選択肢カード | `components/logs/entry-option.tsx` | 入口の 3 択。角丸 18px、左に 48px のアイコン地。主候補は銅地 |
| ログ行 `LogListItem` | `components/logs/log-list-item.tsx` | 62px のカード画像サムネイル（3:4）+ 豆名（display 21px）+ ロースター + 星と数値 + 店名 or 「自宅」ピル + フレーバー最大 3 つ。下線 1px |
| リスト行 `Row` | 汎用 | 左 40px の丸アバター（頭文字、num 700、銅文字）、中央にタイトルと補助、右に数値または chevron |
| データグリッド | `components/beans/bean-spec-grid.tsx` | 2 列、1px の `--line` で区切り、角丸 14px。ラベル 10px `--mute`、値 num 600 15px、溢れは省略記号 |
| カード画像 | `components/beans/card-image.tsx` | 3:4。画像があれば表示、無ければ `--card-paper` 地にロースター・豆名・生産国を印刷物風に描く（Helvetica）。読み込み中はスケルトン |
| トースト | shadcn `sonner` | 上部、地 `--text` 文字 `--bg`、緑のチェック。「保存しました」など操作名と同じ言葉。3 秒 |
| ボトムシート | `components/bottom-sheet.tsx` | 地 `--bg`、上辺角丸 22px、上端に 40×4px のグラバー |
| ステップ表示 | ウィザード共通 | `2 / 3` を num 600 12px `--mute` で右上に |
| スケルトン | shadcn `Skeleton` | `--sf2` と `--line` のグラデーションを 1.4s で流す。`prefers-reduced-motion` では静止 |
| 空状態 | `components/empty-state.tsx` | 96px の丸（`--sf2`）に銅の線画アイコン、見出し 17px、説明 13px `--mute`、主ボタン 1 つ |
| 失敗表示 | `components/error-callout.tsx` | 枠 `--bad`、地は `--bad` 10%、角丸 14px。見出し（`--bad`）+ 何が起きたかと次にできることを 1 文ずつ。謝らない |

---

## 5. 画面仕様

各画面は「要素 / 状態 / 遷移 / 要件 ID / フェーズ」で書く。フェーズは REQUIREMENTS.md §10。

### S1 ログイン `/login` — Phase 0

- 要素: ワードマーク（display 64px、画面下寄せ）、タグライン、「Google で続ける」（白ボタン）、メール入力 + 「ログインリンクを送る」（二次ボタン）、注記「パスワードはありません…」。
- 状態: 送信中（ボタンをスピナーに）、送信完了（「リンクを送りました。メールを開いてください」に置き換え）、失敗（錆色のコールアウト）。
- 遷移: 成功 → S2。ログイン済みなら `/login` に来ても S2 へ。
- 要件: F-AUTH-1。

### S2 入力記録一覧（ホーム）`/` — Phase 1

- 要素: サイトヘッダー、タイトル「入力記録一覧」+ 今月の杯数、検索欄（常設）。絞り込みチップ（絞り込み / すべて / 星 4 以上 / 自宅 / 生産国 / 精製 / タグ）。日付見出し（「今日 · 9月21日（日）」）でグループ化したログ行。
- 状態: 読み込み（ログ行のスケルトン 4 件）、空（「まだ記録がありません」+「最初の記録を追加」）、失敗（コールアウト + 再読み込み）、絞り込み結果 0 件（「条件に合う記録はありません」+ 絞り込みを解除）。
- 遷移: ログ行 → S5。検索欄はフリーワード検索（Phase 5 の F-LIST-3、それまでは非表示）。ヘッダーの「＋ 記録する」→ S3 入口。
- 要件: F-LIST-1 / F-LIST-2 / F-LIST-5。

### S3 記録作成ウィザード `/logs/new` — Phase 1（撮影・OCR は Phase 2）

**入口（`/logs/new`）**
- 要素: ヘッダー（「やめる」）、タイトル、「カードを撮る」（銅、Phase 2 まで非表示）、「手で入力する」、「登録済みの豆から」の 3 択。下に「最近の豆」3 件。
- 遷移: 撮る → ①、手入力 → ②（空フォーム）、登録済み → 豆検索 → ③。最近の豆をタップ → ③。
- 要件: F-LOG-1 / F-BEAN-15 / UC6。

**① カード撮影（Phase 2）**
- 要素: ステッパー（1 撮影）、表のタイル（撮影済みならサムネイル + 撮り直す）、裏のタイル（任意。「カメラで撮る」「写真を選ぶ」）、主ボタン「この内容で読み取る」、注記「長辺 1,600px に縮小」。Web はアプリ内カメラを持たず、OS のカメラを起動する。
- 状態: 表が未撮影なら主ボタンは無効。カメラ権限拒否 → 「写真を選ぶ」だけを残して説明。アプリ内のガイド枠と連続撮影は iOS 版のみ（§9.3）。
- 実装: `components/logs/capture-step.tsx`。`src/lib/platform/camera.ts` 経由で OS のカメラ／写真選択を開き、撮影後 `src/lib/image/compress.ts` で長辺 1,600px。画像は data URL で sessionStorage に置いて ② へ渡す。
- 要件: F-OCR-1 / F-BEAN-12。

**② 読み取り結果の確認・修正 / 手入力フォーム**
- 要素: ヘッダー（「やめる」）、タイトル、ステッパー（2 確認）、注記 + 「手入力に切り替える」リンク、表裏画像（2 列）、フィールド: 豆名（必須）/ ロースター（必須、共通マスタから補完）/ 生産国・標高 / 地域 / 品種・精製 / フレーバー（タグ入力、+ 追加）/ 味覚チャート / 価格・入手区分 / 説明文。主ボタン「次へ：どこで飲んだ？」。
- 状態: OCR 実行中（画像を残したままスケルトン + 進捗バー「通常 3〜8 秒」+「スキップして手入力する」。10 秒で自動的に手入力へ）、OCR 失敗（コールアウト「文字が判別できませんでした」+ 撮り直す / 手で入力する。画像は保持）、必須未入力（豆名の下に錆色の 1 文）。
- 実装: `/logs/new?step=ocr`。`features/ocr/extract-bean-card.ts` で `/api/ocr` を呼び、`extractionToBeanForm` で `BeanForm` の `defaultValues` と `confidence` に流すだけ。`BeanForm` は `confidence` のある項目を `OcrField` で描く。上限超過（429）・無効（503）・10 秒超過は同じ画面で手入力に切り替え、画像は保持する。
- 要件: F-OCR-2 / F-OCR-5 / F-OCR-6 / F-BEAN-1〜11。

**③ 場所・日付・評価・メモ**
- 要素: ヘッダー（「やめる」）、タイトル、ステッパー（3 店と評価）、セグメント（店で / 自宅で）。
  - 店で: 「店で」を押す（またはプレースホルダ「店を選ぶ」）と全画面の**店の検索シート**が開く（Instagram の場所検索の作法。`components/shops/shop-picker.tsx`、ネイティブ `<dialog>`）。上に検索欄（店名・住所）、一番上に「自分で登録する」（検索語があれば「「○○」を自分で登録する」）、その下に「カードのロースター「○○」の場所」（②で読んだロースター名で開いた瞬間に候補を先読み。同名の店があるので住所を見て本人が確定する）、「現在地から探す」、「登録済みの店」（検索語で絞り込み）、「近くの店」、「地図で見つかった店」（2 文字以上、450ms のデバウンスで /api/geo）。「自分で登録する」は店名の手入力 + チップ「地図で指定」+「この店名で決定」。選択後は店カード（銅の枠線。押すと選び直し）。店が無くても保存可（`shop_id` NULL）。
  - 自宅で: 店の欄が消える。自家焙煎豆なら焙煎バッチの選択カード（新規バッチ登録の導線つき）。レシピ（折りたたみ、既定は閉、右に「前回のレシピを複製」）: 器具 / グラインダー / 挽き目 / 湯温 / 豆量 / 湯量 / 比率（自動、num 800 26px）/ 抽出時間 / レシピメモ。
  - 共通: 日付（既定 今日）、星（44px + 数値 36px + ヒント「星の左半分で .5、右半分で .0」）、メモ（84px の複数行）、タグ（使用済みは塗り、候補は点線）、飲み方チップ。主ボタン「保存する」。
- 状態: 保存中（ボタンをスピナー、二重送信防止）、保存失敗（コールアウト、入力は保持）、保存成功（トースト「保存しました」→ S4）。
- 要件: F-SHOP-2/3/4/6/7 / F-LOG-2/3/4/8 / F-TAG-1/2 / F-BREW-1〜7 / F-ROAST-1〜5。店候補（F-SHOP-3/4/7）は Phase 3、レシピ・焙煎は Phase 4。それまではチップと折りたたみを出さない。

### S4 豆詳細 `/beans?id=…` — Phase 1（ADR 0008: 動的セグメントは使わない）

- 要素: サイトヘッダー、パンくず `記録 › 豆` と「編集」リンク。104px のカード画像 + ロースター（num 11px 銅、字間 .14em）+ 豆名（display 34px）+ 平均星と回数。データグリッド（生産国 / 地域 / 品種 / 精製 / 標高 / 価格）。フレーバータグ。説明文（13px `--mute`）。味覚レーダー。「この豆の記録」リスト（日付 · 場所、飲み方 · メモ冒頭、右に星）。主ボタン「この豆をもう一度記録する」。
- 状態: 読み込み（スケルトン）、記録 0 件（豆はあるが記録が無い場合は「まだ記録がありません」の小さな空状態）、失敗。
- 遷移: 編集 → 豆編集フォーム（②と同じフォーム）。記録行 → S5。主ボタン → S3 ③（豆を固定）。
- 要件: F-LIST-4 / F-BEAN-10 / F-BEAN-12。

### S5 記録詳細・編集 `/logs?id=…` — Phase 1（ADR 0008）

- 要素: サイトヘッダー、パンくず `記録 › 日付` と「編集」リンク。日付見出し、豆のサマリ行（サムネイル + 豆名 + ロースター・生産国・精製 + 「豆の詳細を見る」リンク）、データグリッド（場所 / 飲み方、自宅ならレシピ列も）、星（44px + 数値）、メモ、タグ、「同じ豆のほかの記録」、最下部に「この記録を削除」（破壊ボタン）。
- 状態: 削除確認ダイアログ（「この記録を削除しますか？ 豆の情報は残ります」）、削除後トースト「削除しました」→ S2。
- 遷移: 編集 → S3 ③ をこの記録の値で開く。
- 要件: F-LOG-7 / F-LOG-2/3。

### S6 記録したお店 `/shops` — Phase 1

- 要素: サイトヘッダー、タイトル「記録したお店」+ 件数と座標なし件数、「＋ お店を登録」リンク。検索欄。種別チップ（すべて / カフェ / ロースター / 生豆 / 評価順）。行: アバター + 店名 + 種別 · 市区 · 回数、右に平均星。座標なしはピルで示す。
- 状態: 読み込み、空（「店はまだありません。記録を作るときに店を選ぶと増えます」）、失敗。
- 遷移: 行 → 店詳細（店情報 + その店の記録一覧。S4 と同じ構造）。＋ → 店フォーム（店名必須、住所、種別、座標は住所から / 地図タップ）。
- 要件: F-SHOP-1 / F-SHOP-5 / F-SHOP-6 / F-MAP-2。

### S7 記録したお店のマップ `/map` — Phase 3（実装済み 2026-09-22）

- 要素: サイトヘッダー、タイトル「記録したお店のマップ」+ 座標のある店数、「お店の一覧へ」リンク。ページ内 300px の地図枠（Leaflet + OSM、`dynamic(..., { ssr: false })`。現在地ボタン 44px、左下に星色の凡例、ピンは平均星で 4 段階の色、中に記録件数、選択中は 22px の半透明リング）。地図の下に選んだ店のカード（店名 · 種別 · 住所 · 回数、平均星、「ここで飲んだ豆」タグ、「店の詳細」「ここで記録」）、近い順の店一覧、フッター。ボトムシートは使わない。
- 状態: 読み込み（地図タイルの上に薄いスケルトン）、店 0 件（中央に「座標のある店がまだありません」+ 店を追加）、位置情報拒否（現在地ボタンを押したときだけ説明）、オフライン（タイル未取得の旨を上部に）。
- 操作: 長押しでピンを置き「ここに店を登録」→ 店フォーム（F-SHOP-7）。
- 要件: F-MAP-1〜4 / F-SHOP-7。

### S8 好みの分析 `/stats` — Phase 5（実装済み 2026-09-22。期間チップは すべて / 1 年 / 6 か月 / 3 か月、月別の記録数を最下部に追加）

- 要素: タイトル「好みの分析」+ 期間チップ。4 タイル（記録 / 豆 / 店 / 平均星、num 800 26px）。「星 4 以上に多い生産国」「精製方法」の横棒（銅、最大値を 100%）。「よく出るフレーバー」タグ（回数つき）。「好みの形」レーダー（星 4 以上の平均 = 銅、全体平均 = 点線）。
- 状態: 読み込み、記録 3 件未満（「記録が 3 件たまると傾向が出ます」）、失敗。
- 要件: F-STAT-1〜4。

### S9 設定 `/settings` — Phase 0（項目は各フェーズで追加）

- 要素: アカウント行（名前、メール、ログイン方式）。読み取り: 今日の回数 `3 / 50` + 進捗バー + 「毎日 0 時にリセット」。表示: 星の入力（タップ / スライダー）。データ: CSV / JSON でダウンロード、位置情報。アカウント: ログアウト。最下部に「アカウントと全データを削除」（破壊ボタン + 説明文）。
- 状態: エクスポート生成中、削除確認（メールアドレスの再入力を求める）。
- 要件: F-AUTH-3 / F-MISC-2。

---

## 6. 状態と文言のルール

| 状態 | 見せ方 | 文言 |
|---|---|---|
| 読み込み | 内容と同じ形のスケルトン。全面スピナーは認証確認のときだけ | なし |
| 空 | 丸アイコン + 見出し + 1 文 + 主ボタン 1 つ | 行動への招待。「まだ〜がありません」+ 次にできること |
| 失敗 | 錆色のコールアウト、入力内容は保持 | 何が起きたか 1 文、次にできること 1 文。謝罪語・曖昧語を使わない |
| 成功 | 上部トースト 3 秒 | ボタンと同じ動詞:「保存する」→「保存しました」、「削除」→「削除しました」 |
| 破壊操作 | 錆色の枠線ボタン + 確認ダイアログ | 何が消えて何が残るかを書く |

文体: 敬体、簡潔、能動。ボタンは「何が起きるか」を書く（「送信」ではなく「ログインリンクを送る」）。

---

## 7. 実装への落とし込み（Claude Code への指示）

順番どおりに進める。1 PR = 1 項目。

1. **トークン** `src/app/globals.css`: `:root` の shadcn 変数を §2.1 のマッピングで上書きし、`.dark` バリアントと `@custom-variant dark` を削除、`html { color-scheme: dark }` を固定。`--radius` は `0.875rem`（14px）。`src/app/layout.tsx` の `themeColor` を `#17120F`、`appleWebApp.statusBarStyle` を `black-translucent` に。`public/manifest.json` の `theme_color` / `background_color` も同じ値に。
2. **書体** `src/app/layout.tsx`: `next/font/google` で `Bodoni_Moda`（`axes: ['opsz']`, weight 500）と `Manrope`（weight 400〜800）を読み込み、CSS 変数 `--font-display` / `--font-num` に割り当てる。`--font-sans` は端末の日本語スタック。`@theme inline` に `--font-display` / `--font-num` を追加し、Tailwind の `font-display` / `font-num` クラスで使う。
3. **下タブ** `src/components/bottom-nav.tsx`: §3 の仕様（＋の持ち上げ、銅、safe-area）に合わせる。
4. **部品** §4 の順で作る。星・味覚チャート・チップ・入力・ボタン・ログ行・空状態・失敗表示を先に。Storybook は使わず、`src/app/(app)/_dev/components/page.tsx` に部品を並べた開発用ページを置いて確認する（本番ビルドからは除外）。
5. **画面** Phase 1 の S2 → S3（入口・②手入力・③店）→ S4 → S5 → S6 → S9 の順。各画面は P4 の 3 状態を同じ PR に含める。
6. **PC レイアウト** §9.1 のブレークポイントを Tailwind の `md:`（768）と `xl:`（1280）で実装する。`(app)/layout.tsx` の `max-w-md` を外し、`md:` でサイドバー、`xl:` で一覧 + 詳細の 3 列グリッドに切り替える。下タブとサイドバーは同じナビ定義から描く。
7. **モックとの整合** 画面を 1 つ実装するごとに `docs/design/` の該当画面と見比べ、差があればどちらかを直してこの文書に反映する。

---

## 9. プラットフォーム別の設計

同じコード・同じトークン・同じ部品。違うのは幅に応じたレイアウトと、その OS でしかできない入口だけ（ADR 0007）。

### 9.1 ブレークポイント（Web）

| 幅 | 呼び方 | ナビ | 列 | 記録の追加 |
|---|---|---|---|---|
| 〜767px | スマホ | 下タブ（§3） | 1 面。詳細は画面遷移 | ウィザード（S3） |
| 768〜1,279px | タブレット・小さい PC | 左のアイコンレール 72px、ラベル無し | 1 面 | ウィザード（S3） |
| 1,280px〜 | PC | 左サイドバー 236px、ラベル付き。上にワードマーク、下に「記録を追加」ボタンとアカウント | 一覧 400px + 詳細（最大 840px、余白 40px） | モーダル 980×700、2 列 |

- 縮小方向: 1,280 未満で詳細ペインが閉じ、一覧の行タップで画面遷移に戻る。768 未満でサイドバーが下タブに戻る。
- 変わらないもの: 部品、色、書体、文言、3 状態、タップ領域 44px。
- PC にカメラ撮影は無い。入口は「写真をドロップ / クリックで選ぶ」「手入力」「登録済みから」。ドロップした画像は同じ圧縮・同じ OCR を通る。

### 9.2 PC 版の画面（D1〜D4）

| # | 画面 | 構成 | 要件 ID |
|---|---|---|---|
| D1 | ホーム | サイドバー + 一覧（検索欄常設、チップ、日付グループのログ行、選択行は `--sf` の地）+ 詳細（左 260px にカード原本とレーダー、右に豆名 44px・星・データグリッド・フレーバー・説明・記録履歴・主ボタン） | F-LIST-1〜5 |
| D2 | 地図 | サイドバー + 店一覧 360px + 全面地図。ピンの横に 340px のポップカード（店名・種別・回数・平均星・飲んだ豆・「店の詳細」「ここで記録」）。一覧とピンは選択が連動。右上の＋は地図クリックで店を置く | F-MAP-1〜4 / F-SHOP-7 |
| D3 | 統計 | サイドバー + 3 列: 生産国と精製の棒 / 好みの形レーダーと頻出フレーバー / 月別推移（棒 = 件数、線 = 平均星）と星 4.5 以上の豆。期間チップで全グラフが連動 | F-STAT-1〜4 |
| D4 | 記録を追加 | ホームの上にモーダル。左: ドロップ領域 + 豆フィールド（要確認の琥珀はそのまま）、右: 店 / 自宅、店、日付・飲み方、星、メモ、タグ、右下にキャンセルと保存 | F-LOG-1 / F-OCR-2 |

### 9.3 iOS アプリ版

Capacitor で同じコードを包む。差分はすべて `src/lib/platform/` のラッパーで切り替え、UI コンポーネントは OS 分岐を持たない（REQUIREMENTS.md §13.2 N-4）。

| 要素 | Web スマホ（PWA） | iOS アプリ |
|---|---|---|
| 画面・部品・色・書体 | — | 同じ |
| ナビゲーション | サイトヘッダー + ドロワー、フッター、ブラウザの戻る（§3） | 下タブ（ホーム / 地図 / ＋ / 好み / 設定。＋は 56px の銅の円を持ち上げ）、大タイトル 34pt（スクロールで縮む）、戻るは左端スワイプ |
| カード撮影 | OS の標準カメラ、1 枚ずつ | アプリ内カメラ。ガイド枠、四辺検出で枠が銅に、表 → 裏を続けて撮る、「裏は省略」 |
| 写真からの登録 | ギャラリーから選ぶ | 写真アプリの共有シート → 取り込み画面（1 枚目を表、2 枚目を裏に仮置き、入れ替え可）→ S3 ② |
| 行の操作 | タップ | 長押しでコンテキストメニュー: 開く / この豆をもう一度記録 / 前回のレシピで記録 / 編集 / 削除（赤） |
| 削除の確認 | ダイアログ | アクションシート。本文に「何が消えて何が残るか」、削除は赤 |
| 星の入力 | タップ | タップ + 0.5 ごとに軽いハプティクス |
| アイコン・起動画面 | ホーム画面に手動追加 | エスプレッソ地に傾けたカードと銅のドット 5 つ。起動画面はアイコン + ワードマーク、0.5 秒 |
| 認証の戻り先 | https://…/auth/callback | `coffeelog://auth/callback` を追加登録 |

iOS 版の画面 I1〜I7 はモック `docs/design/ios.html` を参照。Phase 6 で作るもの（共有シート受け口、アプリ内カメラ、アイコン原本、認証の戻り先）は同ページ末尾に列挙。

## 10. 変更履歴

| 日付 | 版 | 内容 |
|---|---|---|
| 2026-09-21 | v1.0 | 初版。モック v3（ダーク専用、書体 A）にもとづく |
| 2026-09-22 | v1.1 | §9 プラットフォーム別の設計を追加（Web PC 版 D1〜D4、iOS 版 I1〜I7）。モックを 4 ページに分割 |
| 2026-09-22 | v1.2 | Web スマホ版を Web サイトの作法に変更（§3）。下タブを廃止し、サイトヘッダー + ドロワー、フッター、ステッパー、OS カメラ、地図の流し込み。下タブは iOS 版のみ |
| 2026-09-22 | v1.3 | ページ名とナビ文言を説明的に統一: 入力記録一覧 / 記録したお店 / 記録したお店のマップ / 好みの分析 / 設定。ヘッダーの主ボタンは「＋ 記録する」 |
| 2026-09-24 | v1.4 | S3 ③ の店選びを全画面の検索シートに変更（「自分で登録する」を最上段、カードのロースター名から候補を先読み）。画面のズームを固定 |
