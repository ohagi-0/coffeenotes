# 2026-09-24 作業ログと引き継ぎ（微修正ラウンド）

このチャットは 1 日で多くの変更を入れたため、次のセッション向けに要点を残す。詳細は各コミットと `docs/DESIGN.md` v1.4〜v1.9 の変更履歴を参照。

## 今日入れたもの（main、すべて本番反映済み）

| 領域 | 内容 | 主なコミット |
|---|---|---|
| OCR | strict ツールスキーマをフラット形式に組み替え（入れ子だと grammar too large）。実 API で録画。タイムアウト 30 秒、`/api/ocr/warm` + GitHub Actions（12 時間ごと）で文法キャッシュを温める。10 秒での手入力自動切替は廃止 | b33287f ecc1051 55393de |
| 記録作成 | 店選びを全画面の検索シートに（`components/shops/shop-picker.tsx`）。並びは 地図の結果 → 近く → 登録済み（該当）→ 自分で登録。カードのロースター名で候補を先読み。ロースターは任意に。入口は「テイスティングカードから読み取る」、「登録済みの豆から」は検索して選ぶ | a8a9590 deba6c6 ほか |
| 保存 | 画像アップロードを並列化、タグ付けの往復削減、進捗表示 | c95c4be |
| 一覧 | 一括削除（選択モード）、フリーワード検索（`?q=`）、行の画像は常に印刷物風 | 93fa24b ae21c11 |
| 店 | 地図と一覧を `/shops` に統合（`/map` は転送）。検索語に該当が無ければその場所へ地図を寄せる。Google マップで開く | 7d3f69d（マージ）bb2efb1 |
| ホーム | 豆のコレクション（棚）を `/` に。一覧は `/logs`。袋は写真なしのラベル型。生産国の棚（27 か国、空き棚つき）が既定、品種の棚（28 種）も | 1ff9d57 13e4f63 |
| その他 | ズーム固定、焙煎度チップ + 分析タイル、フッター圧縮 | cab8235 5f5acd1 eeace6d |

## ロールバック

- 地図統合だけ戻す: `git revert -m 1 7d3f69d`。統合前の main はタグ `before-shops-map-merge`。
- ホームの入れ替え（棚 ⇄ 一覧）を戻す: `src/components/nav.ts` の href と `src/app/(app)/page.tsx` / `src/app/(app)/logs/page.tsx`（id なしで `LogListView`）を見る。旧 URL `/collection` は `/` へ転送。

## 未完了・要確認

- **CLAUDE.md と docs/USER-SETUP.md** に別ウィンドウの未コミット編集が残っていた（Google ログイン済み、Vercel×GitHub 連携済み、品質ゲートの説明、E2E 用の環境変数）。このチャットでは巻き込まないよう触っていない。CLAUDE.md §3 の構成図と §5.3 のナビ文言は現状（ホーム = 棚、`/logs` = 一覧、ナビ 5 項目、`/map` は転送）に合わせて直す必要がある。
- モック `docs/design/*.html` は文言だけ更新。S6（地図 + 一覧）、S10（棚）、店の検索シート、袋の絵は描いていない。
- 店候補は Nominatim なので、店名の一部では見つからないことがある。Overpass の名前検索や Google Places への切り替えは未着手（`GeoProvider` の抽象は維持）。
- 生産国・品種の別名表（`features/beans/collection.ts`）は手作り。実データで棚に入らない表記が出たら aliases に足す。
- 一覧のフリーワード検索とコレクションは全記録をクライアントで処理している。数百件までは問題ない想定。
- E2E 用ユーザー（`E2E_TEST_EMAIL`）には確認のためサンプルデータを投入したまま（`pnpm seed:dev --email <E2E> --reset --yes` で入れ直せる）。
- 実機で未確認: 保存の体感速度、店の検索シートの使い心地、棚の質感と文字サイズ、入口の長い文言が 2 行になっていないか。

## 次の候補（ユーザーの発言から）

- 棚の「特別感」をさらに上げる（袋の質感、ラベルの余白、封蝋）。
- 棚に入れる豆を自分で選ぶ（お気に入りだけを飾る）案が一度出た。
